import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'red',
    padding: 8,
    borderRadius: 8,
  },
  text: {
    fontSize: 24,
  },
});

export default styles;
