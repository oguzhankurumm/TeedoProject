import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    rowGap: 20,
  },
  productImage: {
    width: '100%',
    aspectRatio: 16 / 9,
  },
});

export default styles;
